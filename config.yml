title: Vidio call sex
description: 'Yes'
template:
  repo: vidio-call-sex-template
  name: vidio-call-sex
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
